export enum TaskStatus {
  PENDING = 'Pending',
  DONE = 'Done',
  INPROGRESS = 'InProgress',
  PAUSED = 'Paused',
}

export enum TaskPriority {
  HIGH = 'High',
  MEDIUM = 'Medium',
  NORMAL = 'Normal',
}
